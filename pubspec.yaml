name: cimpl
description: The Cimpl.credit companion app

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  http: ^0.12.0
  intl: ^0.15.7
  charts_flutter: ^0.5.0
  firebase_messaging: 2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.7.0"

flutter:
  uses-material-design: true
  assets:
    - assets/CimplLogo4.png

flutter_icons:
  android: "launcher_icon"
  ios: false
  image_path: "assets/CimplIcon.png"